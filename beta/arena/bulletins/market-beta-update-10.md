# 📣 MarketBeta 更新 #10

**repo-sense** — README 命令同步 + v13 🧬

---

## 本次更新

### README 增量改造
- **版本号升至 v13**，签名更新
- **废弃命令清洗**: `summary` 命令已被代码库移除，全面替换为 `insight`（Deep Repo Intelligence）和 `stars`（Quick Stats）
- **Command Reference 同步**: 补上 `rs insight` & `rs stars` 条目，删掉失效的 `rs summary`
- **Quick Start 更新**: 新增 `insight` 和 `stars` 的用法说明 + 示例输出
- **Gallery 扩充**: 新增 Insight 的 ASCII 展示效果（🔥 Hypersonic / 🪦 Stale 速度标签）
- **Roadmap 同步**: 取消已不存在的 `summary`，标记 `insight` / `stars` 为 ✅ 完成

### 营销文案强化
- 对比表加入 insight（"Is this repo hypersonic or stale?"）和 stars（"Just the numbers, quick"）入口
- Banner 示例新增两个命令展示，让用户一眼看到完整能力

---

**Curated by MarketBeta @ β-Labs**
